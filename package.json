{
  "name": "indspace",
  "version": "0.1.0",
  "homepage": "https://ToMattBan.github.io/IndSpace",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.5.0",
    "@testing-library/user-event": "^7.2.1",
    "onchange": "^7.0.2",
    "prettier": "^2.1.2",
    "react": "^17.0.0",
    "react-dom": "^17.0.0",
    "react-router-dom": "^5.2.0",
    "react-scripts": "3.4.4"
  },
  "scripts": {
    "start": "npm run pWatch & npm run startProject",
    "startProject": "react-scripts start",
    "deploy": "npm run build && gh-pages -d build && git push",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "pWatch": "onchange ./src -- prettier --write --ignore-unknown {{changed}}"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
